name: C_C++_CI

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    uses: ./.github/workflows/c-cpp_tmpl.yml
    with:
      dependencies: >
        gcc gcovr openblas cblas lapacke scalapack boost eigen3 openmpi pyscf
        cppyy
    secrets:
      CPP_GITHUB_TOKEN: ${{ secrets.CPP_GITHUB_TOKEN }}
